    #spring.datasource.url=jdbc:mysql://localhost:3306/myblog?useSSL=false&serverTimeZone=UTC
    #spring.datasource.username=root
    #spring.datasource.password=test

#hibernate properties #using dialect im telling to hibernate which database SQL Query it have to generated
    #spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

#hibernate ddl auto
    #spring.jpa.hibernate.ddl-auto=update

#Spring.security.user.name=Saanu
#Spring.security.user.password=test
#Spring.security.user.roles=ADMIN

server.port=5000

#App Properties
#it can be anything
app.jwt-secret= JWTSecretKey
#it is life shale time (it is time for expire ----- it will expire in 7 days)
app.jwt-expiration-milliseconds = 604800000

#application-prod.properties file will be scaned here
spring.profiles.active=prod

